import {
  initScopeButtons,
  initScanButton,
  initMigrateButton,
  initClearAllButton,
  initResetButton,
  initCopyButton,
  initDetachButton,
  initMessageListener,
} from './handlers.js';

// ── Bootstrap ─────────────────────────────────────────────────────────────────

initScopeButtons();
initScanButton();
initMigrateButton();
initClearAllButton();
initResetButton();
initCopyButton();
initDetachButton();
initMessageListener();
